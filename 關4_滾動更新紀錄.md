# 關 4：改自動部署到 ECS Fargate（滾動更新）

**日期：** 2026-08-16
**目標：** 解決關 3 剩下的最後一個問題——**換版瞬間服務中斷**
**結果：** 部署期間持續請求 193 次，**失敗 0 次，可用率 100%**

---

## 關 3 剩下的問題

關 3 的部署動作是：

```bash
docker rm -f nkc202app     # 先殺掉舊容器 ← 服務從這裡開始中斷
docker run -d ...          # 再啟動新容器 ← 到這裡才恢復
```

中間必然有數秒空窗。單機部署無法避免——**只有一個容器，殺掉就沒了。**

在金融業，這對應到「上線需要停機窗口」：必須排在收盤後或週末，動員人力待命。

---

## 關 4 的解法：滾動更新

```yaml
DeploymentConfiguration:
  MinimumHealthyPercent: 100   # 部署期間健康任務數不得低於原本的 100%
  MaximumPercent: 200          # 允許暫時擴充到 200%
```

這兩個數字合起來的效果：

```
原本：  [舊任務A] [舊任務B]                    ← 2 個
步驟1： [舊任務A] [舊任務B] [新任務C] [新任務D]  ← 暫時擴到 4 個
步驟2： 等待新任務通過 ALB 健康檢查
步驟3： [新任務C] [新任務D]                    ← 舊任務逐一下線
```

**全程至少維持 2 個健康任務，使用者不會遇到中斷。**

搭配 Target Group 的 `deregistration_delay.timeout_seconds: 15`——舊任務先停止接收新連線，等既有連線處理完才終止，不是硬砍。

---

## 零停機驗證（本關的核心證據）

**方法：** 部署期間以每 0.5 秒一次的頻率持續請求 ALB，記錄每次的 HTTP 狀態碼與回傳版本。

### 結果

| 項目 | 數值 |
|---|---|
| 總請求數 | **193 次** |
| HTTP 200 | **193 次** |
| 失敗（非 200 或連線失敗） | **0 次** |
| **可用率** | **100.00%** |

### 換版瞬間的明細

```
時間        狀態  版本
15:11:26    200   v1.2.13-ci   ← 舊版
15:11:27    200   v1.2.14-ci   ← 新任務通過健康檢查，開始分流
15:11:28    200   v1.2.13-ci   ← 舊任務仍在服務
15:11:29    200   v1.2.13-ci
15:11:31    200   v1.2.14-ci
15:11:32    200   v1.2.13-ci
     ⋮      兩版並存約 15 秒，流量逐步轉移
15:11:47    200   v1.2.14-ci   ← 舊任務全數下線，切換完成
```

**新舊版本同時服務約 15 秒，期間沒有任何一個請求失敗。**

這個交錯模式就是滾動更新的指紋——它證明系統是「先起新的、確認健康、再收舊的」，而不是「先殺再起」。

---

## 架構變更

| | 關 3 | 關 4 |
|---|---|---|
| 運算 | EC2 單機、單一容器 | ECS Fargate、2 個任務 |
| 可用區 | 僅 1a | **1a + 1c** |
| 流量入口 | Elastic IP 直連 | **Application Load Balancer** |
| 健康檢查 | 容器層 HEALTHCHECK | **ALB 主動探測 `/healthz`** |
| 換版方式 | 殺掉重起 | **滾動更新** |
| 換版中斷 | 有（數秒） | **無** |
| 主機管理 | 需管作業系統、Docker 版本 | **無主機可管（Serverless）** |

---

## 設計決策

| 決策 | 選擇 | 理由 | 代價 |
|---|---|---|---|
| **Fargate 任務放公有子網並配發公有 IP** | 而非私有子網 + NAT | 任務需拉取 ECR 映像檔。NAT Gateway 約 US$32/月、VPC Endpoints × 3 約 US$21/月，本方案 US$0。安全性由 Security Group 保證：只接受來自 ALB 的 8080，外部無法直接連入 | 正式環境仍建議私有子網 + VPC Endpoints |
| **任務數 2 而非 1** | 跨 AZ 分散 | 單一可用區故障時服務不中斷；滾動更新的畫面也更清楚 | Fargate 費用加倍（約 US$0.74/日） |
| **保留 EC2 部署不移除** | 兩個目標並存 | 驗收時可同時展示關 3 與關 4 的差異，對照更有說服力 | 兩份部署邏輯需維護 |
| **Task Role 不掛任何政策** | 空角色 | 本應用不呼叫任何 AWS API，不需要就不給 | 無 |
| **CloudWatch Container Insights 關閉** | 停用 | 會產生額外 CloudWatch 費用，專題不需要 | 少了容器層細部指標 |
| **日誌保留 7 天** | 而非預設永久 | 專題用途足夠且省錢 | 超過 7 天的日誌會消失 |

---

## 權限設計（延續最小權限原則）

GitHub Actions 角色在關 4 新增的權限，仍然精確限定範圍：

| 動作 | 資源範圍 |
|---|---|
| `ecs:DescribeTaskDefinition` `ecs:RegisterTaskDefinition` | `*`（AWS 此二 API 不支援資源層級限制） |
| `ecs:UpdateService` `ecs:DescribeServices` | **僅** `nkc202-cicd-service` 這一個服務 |
| `iam:PassRole` | **僅** ECS 的兩個角色，且附加條件 `iam:PassedToService = ecs-tasks.amazonaws.com` |

該角色至今累積三條政策，每一條都對應一個明確的階段需求：

```
ecr-push-nkc202-cicd-app      關 2 加入——只能推特定 ECR 倉庫
ssm-deploy-single-instance    關 3 加入——只能對特定 EC2 下指令
ecs-deploy-single-service     關 4 加入——只能更新特定 ECS 服務
```

**需要時才給，不預先授予。**

---

## 費用

| 項目 | 單價 | 到 9/5（約 20 天） |
|---|---|---|
| Application Load Balancer | 約 US$0.58/日 | 約 US$12 |
| Fargate 2 任務（0.25 vCPU / 0.5 GB） | 約 US$0.74/日 | 約 US$15 |
| **合計** | 約 US$1.32/日 | **約 US$27** |

> 將任務數改為 1 可省下約 US$7.5，滾動更新仍然無中斷（部署時會暫時擴充到 2 個），但失去跨 AZ 容錯。

---

## 累計對照表

| | 人工步驟 | 端到端耗時 | 人工介入 | 測試關卡 | 版本可追溯 | 換版中斷 |
|---|---|---|---|---|---|---|
| **關 0** 手動部署 | 18 步 | 16 分鐘 | 每次都要 | ❌ | ❌ | 有 |
| **關 1** 容器化 | 7 步 | 37 秒 | 每次都要 | ❌ | ✅ 手動 | 有 |
| **關 2** 自動建置 | 1 步 | 56 秒 | 零 | ✅ | ✅ 自動 | 有 |
| **關 3** 自動部署 | 1 步 | 80 秒 | 零 | ✅ | ✅ 自動 | 有 |
| **關 4** 滾動更新 | 1 步 | 約 2 分鐘 | 零 | ✅ | ✅ 自動 | **無（實測可用率 100%）** |

> 關 4 的端到端耗時比關 3 長，是因為滾動更新必須等新任務通過健康檢查才能收舊任務。
> **這段時間是為了換取「零中斷」而刻意付出的成本，不是效能退步。**

---

## 對應金融業的價值

| 手動部署時代的做法 | 關 4 之後 |
|---|---|
| 上線必須排停機窗口（收盤後、週末） | **營業時間內即可上線** |
| 需 IT 與業務同時待命 | 無需待命 |
| 中斷期間客戶下不了單 | 客戶無感 |
| 單機故障即全面中斷 | 跨可用區，單一機房故障仍可服務 |

對應內控術語：**營運持續（BCP）** 與 **服務水準（SLA）**。
「上線不需要停機」在券商是很實際的訴求——每一次停機窗口都是人力成本與客訴風險。
