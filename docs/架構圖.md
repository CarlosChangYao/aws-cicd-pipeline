# 系統架構圖

> 本檔案的圖以 Mermaid 撰寫，GitHub 會自動渲染。
> 修改圖形只需改文字，不需重新繪製與上傳圖檔——這本身也是「基礎設施即程式碼」思維的延伸。

---

## 圖一：完整架構（關 3 完成後的狀態）

```mermaid
flowchart TB
    subgraph DEV["開發者本機"]
        CODE["程式碼<br/>app.py / Dockerfile"]
    end

    subgraph GH["GitHub"]
        REPO["Repository<br/>CarlosChangYao/aws-cicd-pipeline"]
        subgraph ACT["GitHub Actions"]
            J1["job 1：執行測試<br/>pytest"]
            J2["job 2：建置並推送<br/>docker build → ECR"]
            J3["job 3：部署到 EC2<br/>SSM Run Command"]
        end
    end

    subgraph AWS["AWS ap-northeast-1（東京）"]
        OIDC["IAM OIDC Provider<br/>信任 GitHub，簽發 1 小時臨時憑證"]
        ROLE["IAM Role<br/>nkc202-github-actions<br/>僅限單一 ECR 倉庫與單一 EC2"]
        ECR["Amazon ECR（私有）<br/>nkc202-cicd-app<br/>推送時掃描漏洞・保留 5 版"]

        subgraph VPC["VPC 10.0.0.0/16"]
            IGW["Internet Gateway"]
            subgraph PUB["Public Subnet 10.0.1.0/24（AZ 1a）"]
                EC2["EC2 t3.micro<br/>Docker 容器（非 root）<br/>SG：僅開 80・sshd 已停用"]
            end
            subgraph PRI["Private Subnet 10.0.11.0/24・10.0.12.0/24"]
                RSV["（預留，無對外路由）"]
            end
        end
        SSM["Systems Manager<br/>Run Command / Session Manager"]
    end

    USER(["使用者瀏覽器"])

    CODE -->|"git push（唯一人工動作）"| REPO
    REPO -->|"push 事件觸發"| J1
    J1 -->|"測試通過才繼續"| J2
    J2 --> J3

    J2 -.->|"以 OIDC 換取臨時憑證"| OIDC
    OIDC --> ROLE
    ROLE -.-> ECR
    ROLE -.-> SSM

    J2 -->|"推送映像檔"| ECR
    J3 -->|"下達部署指令"| SSM
    SSM -->|"Agent 主動輪詢取得指令"| EC2
    EC2 -->|"以 EC2 IAM Role 拉取映像檔"| ECR

    USER -->|"HTTP 80"| IGW
    IGW --> EC2

    classDef aws fill:#232F3E,stroke:#FF9900,color:#fff
    classDef gh fill:#1f2328,stroke:#8b949e,color:#fff
    classDef sec fill:#1e3a5f,stroke:#38bdf8,color:#fff
    class ECR,EC2,SSM,IGW aws
    class REPO,J1,J2,J3 gh
    class OIDC,ROLE sec
```

---

## 圖二：六關演進（本專題的主軸）

```mermaid
flowchart LR
    G0["<b>關 0</b><br/>手動部署<br/>―――――<br/>18 步<br/>16 分鐘<br/>2 次失敗<br/>版本無法追溯"]
    G1["<b>關 1</b><br/>容器化 + ECR<br/>―――――<br/>7 步<br/>37 秒<br/>0 次失敗<br/>版本手動帶入"]
    G2["<b>關 2</b><br/>自動建置<br/>―――――<br/>1 步<br/>56 秒<br/>零人工<br/>版本機器產生"]
    G3["<b>關 3</b><br/>自動部署<br/>―――――<br/>1 步<br/>80 秒<br/>零人工<br/>對應 commit"]
    G4["<b>關 4</b><br/>ECS Fargate<br/>―――――<br/>滾動更新<br/>服務不中斷"]
    G5["<b>關 5</b><br/>跨雲分發<br/>―――――<br/>評估後不做"]

    G0 -->|"解決：程式碼傳輸<br/>與環境安裝"| G1
    G1 -->|"解決：打包與推送<br/>仍需人工"| G2
    G2 -->|"解決：部署仍需人工"| G3
    G3 -->|"解決：換版瞬間中斷"| G4
    G4 -.->|"投入產出比低"| G5

    classDef done fill:#14532d,stroke:#22c55e,color:#fff
    classDef todo fill:#3f2d12,stroke:#f59e0b,color:#fff
    classDef skip fill:#3f1d1d,stroke:#ef4444,color:#fff
    class G0,G1,G2,G3 done
    class G4 todo
    class G5 skip
```

---

## 圖三：認證流程（全程零長期金鑰）

> 這張圖回答的是：**「GitHub 憑什麼能操作你的 AWS？金鑰放在哪裡？」**
> 答案是：**沒有金鑰。** 每一段都是「用身分換取短期憑證」。

```mermaid
sequenceDiagram
    autonumber
    participant GA as GitHub Actions
    participant GH as GitHub OIDC 簽發端
    participant STS as AWS STS
    participant ECR as Amazon ECR
    participant SSM as AWS SSM
    participant EC2 as EC2 主機

    GA->>GH: 請求身分權杖
    GH-->>GA: 簽發 JWT（數分鐘有效）<br/>sub=repo:CarlosChangYao@.../aws-cicd-pipeline@...
    GA->>STS: AssumeRoleWithWebIdentity（出示 JWT）
    STS-->>GA: 臨時憑證（1 小時後失效）

    GA->>ECR: 以臨時憑證取得推送權杖
    GA->>ECR: 推送映像檔

    GA->>SSM: SendCommand（僅限指定的那一台 EC2）
    Note over EC2: EC2 上的 Agent 主動輪詢<br/>不需開放任何 inbound port
    EC2->>SSM: 取得待執行指令
    EC2->>ECR: 以 EC2 IAM Role 拉取映像檔
    Note over EC2: 重啟容器，完成部署
```

---

## 關鍵設計決策

| 決策 | 選擇 | 理由 | 代價 |
|---|---|---|---|
| **不建 NAT Gateway** | 私有子網無對外路由 | 私有子網目前無主動對外需求 | 省 US$32/月，且少一個攻擊面 |
| **私有子網用獨立路由表** | 路由表僅有 `local` | 「出不去」由路由層保證，不倚賴防火牆規則 | 無 |
| **不開 22 port 且停用 sshd** | 管理走 SSM | 縱深防禦：即使 SG 被誤改，主機也無 SSH 服務 | 無 |
| **OIDC 而非 Access Key** | 臨時憑證 | repo 內零長期金鑰，外洩風險大幅降低 | 設定較複雜（本專題卡了 6 次執行） |
| **部署指定 commit SHA** | 不使用 `latest` | `latest` 會被覆蓋，指定 SHA 才能精準對應與回滾 | 無 |
| **容器以非 root 執行** | 建立 `appuser` | 容器遭入侵時攻擊者非 root | 需將監聽埠改為 8080 並對外映射 |
| **ECR 生命週期政策** | 僅保留 5 版 | 避免映像檔無限累積產生儲存費 | 過舊版本無法回滾 |
| **CloudFormation 管理基礎設施** | IaC | 整組建立／整組刪除，避免資源殘留 | 學習成本 |
