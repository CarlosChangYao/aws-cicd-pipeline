# 檢核點 #12 與 #20 驗證紀錄

**日期：** 2026-08-16
**目的：** 補齊兩項「機制已建置但未實際演示」的檢核點

---

## 檢核點 #12：測試失敗時流水線紅燈中止，且不部署

### 驗證方法

刻意將 `app/test_app.py` 的 `test_healthz` 預期狀態碼由 200 改為 201（應用實際回傳 200，因此測試必然失敗），推送後觀察流水線行為，並確認線上服務未受影響。

```python
def test_healthz():
    client = app.test_client()
    resp = client.get("/healthz")
    assert resp.status_code == 201   # ← 刻意寫錯
```

### 執行結果（第 19 次流水線執行）

| Job | 結果 |
|---|---|
| 執行測試 | **❌ 失敗** |
| 建置並推送映像檔 | **⏭️ 未執行（skipped）** |
| 部署到 EC2 | **⏭️ 未執行（skipped）** |
| 滾動更新部署到 ECS | **⏭️ 未執行（skipped）** |

失敗訊息：

```
assert 200 == 201
 +  where 200 = <WrapperTestResponse streamed [200 OK]>.status_code
test_app.py:26: AssertionError
1 failed, 2 passed
```

### 關鍵驗證：損壞的程式碼是否進入線上？

| 檢查項目 | 演示前 | 演示後 | 結論 |
|---|---|---|---|
| EC2 線上版本 | v1.2.18-ci | v1.2.18-ci | **未變更** |
| ECS 線上版本 | v1.2.18-ci | v1.2.18-ci | **未變更** |
| ECR 最新映像檔 | 86adf1a / v1.2.18-ci | 86adf1a / v1.2.18-ci | **未推入新映像檔** |

**損壞的程式碼連映像檔倉庫都未進入，更未抵達線上環境。**

### 恢復

將測試改回 `assert resp.status_code == 200` 並推送，第 20 次流水線執行四個 job 全數通過，線上版本更新為 v1.2.20-ci。

### 意義

此機制由 workflow 中的 `needs` 相依關係實現：

```yaml
jobs:
  test:            # 第一關
    ...
  build-and-push:
    needs: test    # ← test 未通過則本 job 完全不執行
```

在技術上稱為持續整合的品質閘門；若以金融業的語言表述，則為**自動化的內控關卡**——過往變更管理倚賴人工簽核與人工記得檢查，此處改為機器強制執行，操作者無法繞過。

---

## 檢核點 #20：指定舊版本 commit SHA 重新部署（回滾）

### 驗證方法

自 ECR 中選定較舊的映像檔標籤，以相同的部署機制重新部署至 EC2 與 ECS，確認線上版本確實退回。

### ECR 中可用的版本

```
2026-08-16T15:45:25   SHA=b23024e   版本=v1.2.20-ci   ← 當時的線上版本
2026-08-16T15:33:15   SHA=86adf1a   版本=v1.2.18-ci
2026-08-16T15:23:30   SHA=066cdd5   版本=v1.2.17-ci
2026-08-16T15:14:57   SHA=f7f62d4   版本=v1.2.16-ci   ← 回滾目標
2026-08-16T15:14:08   SHA=0b24d85   版本=v1.2.15-ci
```

### 執行結果

| 項目 | 內容 |
|---|---|
| 回滾前版本 | v1.2.20-ci |
| 回滾目標 | `f7f62d4`（v1.2.16-ci），往回 4 個版本 |
| EC2 回滾方式 | SSM Run Command，指定 SHA 標籤拉取並重啟容器 |
| ECS 回滾方式 | 註冊指向舊 SHA 的任務定義，觸發滾動更新 |
| **回滾後 EC2** | **v1.2.16-ci** |
| **回滾後 ECS** | **v1.2.16-ci** |
| 網頁顯示的 Git Commit | `f7f62d4` |
| 總耗時 | **257 秒**（含 ECS 滾動更新等待新任務通過健康檢查） |

回滾完成後再往前復原至 `b23024e`，兩個部署目標均回到 v1.2.20-ci，狀態恢復。

### 這項能力來自哪一個設計決策

回滾之所以可行，源自關 2 的一項刻意設計：**部署時指定 commit SHA 標籤，而非 `latest`**。

| 標籤 | 特性 | 能否用於回滾 |
|---|---|---|
| `latest` | 會被後續每一次建置覆蓋 | **否**——指向的內容不斷改變 |
| `<version>` | 人類可讀，但仍可能重複 | 部分可 |
| **`<commit-sha>`** | **與該次程式碼修改永久對應，不會被覆蓋** | **是** |

因此回滾在操作上並非「特殊程序」，而只是「指定另一個標籤重新部署」——與正常部署走完全相同的路徑。

### 意義

在金融業，此能力對應到**營運持續（BCP）**中的復原時間目標（RTO）。

| | 手動部署時代 | 本專題 |
|---|---|---|
| 舊版本在哪 | 可能找不到，或不確定是哪一版 | ECR 中每個 commit 的映像檔都在 |
| 如何復原 | 重新走一次完整部署流程 | 指定舊 SHA 重新部署 |
| 耗時 | 小時級 | **分鐘級（實測 257 秒）** |

### 限制

ECR 設有生命週期政策，僅保留最近 5 個映像檔。因此可回滾的範圍為最近 5 個版本，更早的版本需重新建置。此為成本與可回溯範圍之間的取捨，正式環境可依需求調整保留數量。

---

## 附帶觀察：一次錯誤的判讀

首次查詢流水線結果時，取得的是「成功」的紀錄，與預期不符。追查後發現該筆為更早一次推送（commit `86adf1a`）的執行紀錄，而非本次（`e92dc38`）——GitHub API 依時間排序回傳，當時本次的執行尚未建立。

以 commit SHA 精確比對後，取得正確的第 19 次執行紀錄，結果與預期一致。

**此事再次印證關 2 除錯時得到的經驗：驗證時必須確認「查到的資料是否為當下要驗證的對象」，而非僅看結果符不符合預期。**
