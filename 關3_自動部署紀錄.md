# 關 3：ECR 更新 → 自動部署到 EC2

**日期：** 2026-08-16
**目標：** 補上關 2 剩下的最後一個斷點——映像檔進了 ECR，但 EC2 還在跑舊版
**結果：** **Tier 3 達標**。`git push` 後 80 秒自動上線，人工介入為零。

---

## 這一關補上了什麼

關 2 完成後的流程仍有斷點：

```
git push → 測試 → build → 推 ECR → 【斷點】→ EC2 仍跑舊版
```

關 3 補上該段：

```
git push → 測試 → build → 推 ECR → SSM Run Command → EC2 拉取並重啟 → 公網健康檢查
                                    └────── 全自動 ──────┘
```

---

## 正式驗證（檢核點 #17）

實際操作：在 `app/app.py` 加入一行程式碼，`git push`，**之後不執行任何動作**。

| 時間點 | 事件 |
|---|---|
| 14:22:49 | `git push`（唯一的人工動作） |
| — | GitHub Actions 自動觸發：測試 → 建置 → 推送 ECR → SSM 部署 |
| 14:24:09 | 線上版本由 `v1.2.8-ci` 變為 `v1.2.9-ci` |
| **總計** | **80 秒** |

驗證線上內容與程式碼一致：

```
線上頁面顯示   Git Commit  dc4758f
本機 git log   dc4758f  Demo：改一行程式碼，驗證自動上線（檢核點 #17）
新增的那行     Deployed Via  GitHub Actions -> ECR -> SSM (no SSH)   ← 已出現在頁面上
```

---

## 設計決策

| 決策 | 做法 | 理由 |
|---|---|---|
| **以 SSM Run Command 部署，不用 SSH** | GitHub Actions 呼叫 `ssm:SendCommand` | EC2 不需開任何 inbound port；GitHub 不需保管主機金鑰；每次部署留下 CloudTrail 稽核軌跡 |
| **部署指定 commit SHA 標籤，不用 `latest`** | `docker pull ...:dc4758f` | `latest` 會被後續建置覆蓋，指定 SHA 才能確保「部署的版本」與「程式碼」永久對應，回滾時也才有依據 |
| **部署後從公網做健康檢查** | `curl $APP_URL/healthz` 並比對版本 | 「SSM 指令成功」不等於「服務正常」。從外部驗證才是閉環 |
| **SSM 權限精確到單一主機** | Resource 限定 `instance/i-0937ad0e32c5fedca` 與 `document/AWS-RunShellScript` | 即使憑證外洩，也只能對這一台機器執行這一種文件 |
| **SSM 權限到關 3 才授予** | 關 2 時刻意只給 ECR 權限 | 最小權限原則：需要時才給，不預先授予 |

---

## 安全性總結（評分維度③）

整條流水線中，**沒有任何一處存放長期憑證**：

| 環節 | 認證方式 | 是否有長期金鑰 |
|---|---|---|
| GitHub Actions → AWS | OIDC 換取臨時憑證（1 小時） | ❌ 無 |
| Actions → ECR | 以上述臨時憑證取得 ECR 權杖（12 小時） | ❌ 無 |
| Actions → EC2 | SSM Run Command（不經 SSH） | ❌ 無 |
| EC2 → ECR | EC2 IAM Role 換取權杖 | ❌ 無 |

其他措施：EC2 不開 22 port 且已停用 sshd（縱深防禦）；容器以非 root 執行；ECR 私有倉庫且啟用推送掃描；映像檔生命週期政策僅保留 5 版。

---

## 六關對照表（報告結論）

| | 人工步驟 | 端到端耗時 | 人工介入 | 測試關卡 | 版本可追溯 | 可回滾 |
|---|---|---|---|---|---|---|
| **關 0　手動部署** | **18 步** | **16 分鐘** | 每次都要 | ❌ 無 | ❌ `none-manual-deploy` | ❌ |
| **關 1　容器化 + ECR** | 7 步 | 37 秒 | 每次都要 | ❌ 無 | ✅ 手動帶入 | ⚠️ 需手動指定 |
| **關 2　自動建置** | 1 步 | 56 秒 | 零 | ✅ 自動阻擋 | ✅ 機器產生 | ⚠️ 需手動部署 |
| **關 3　自動部署** | **1 步** | **80 秒** | **零** | ✅ 自動阻擋 | ✅ 機器產生 | ✅ 指定 SHA 重跑 |

> 關 3 的端到端耗時（80 秒）比關 2 的建置時間（56 秒）長，是因為多了部署與公網健康檢查兩段。
> 但**關 2 的 56 秒之後，人還要自己去部署**；關 3 的 80 秒是「從按下 push 到使用者看得到新版」的完整時間。

**關 0 → 關 3 改善幅度：人工步驟 −94%、端到端耗時 −92%、人工介入歸零、新增自動化測試關卡、版本從無法追溯變為精確對應 commit。**

---

## 對應金融業的價值（報告論述）

| 手動部署的風險 | 內控術語 | 關 3 的解法 |
|---|---|---|
| 18 個步驟仰賴人不出錯 | 作業風險 | 自動化，人只做 1 步 |
| 不知道線上是哪一版 | 稽核軌跡 / 可追溯性 | 每次部署綁定 commit SHA |
| 出錯後復原耗時 | 營運持續（BCP） | 指定舊版 SHA 重新部署即可回滾 |
| 沒有審批與檢查紀錄 | 變更管理 / 內控 | 測試不過流水線紅燈中止，繞不過去；全程留下執行紀錄 |
| 上線需人工登入主機 | 存取控制 | 全程無 SSH，改由 SSM 執行並留下 CloudTrail 紀錄 |

---

## 尚待完成

- [ ] **檢核點 #12**：故意讓測試失敗，展示流水線紅燈中止、不部署
- [ ] **檢核點 #20**：指定舊版 commit SHA 重新部署，驗證回滾能力
- [ ] 關 3 的證據截圖（Actions 三個 job 全綠、部署摘要、線上頁面）
