# 關 0：手動部署紀錄

> 這份紀錄是整個專題的**基準線**。關 1、2、3 的價值全靠跟這裡的數字對照才顯現。
> 時間以截圖檔案的時間戳為準，非事後估算。

**日期：** 2026-08-16
**環境：** EC2 i-0937ad0e32c5fedca（Amazon Linux 2023, t3.micro, ap-northeast-1a）
**連線方式：** SSM Session Manager（未開 22 port、無 SSH 金鑰、sshd 已停用）
**部署內容：** Flask 應用（45 行）+ gunicorn + systemd 服務

---

## 計時結果

| 項目 | 數值 | 依據 |
|---|---|---|
| 開始時間 | **11:40:12** | 截圖 09（Session Manager 連線成功） |
| 部署完成 | **11:56:15** | 截圖 10（Mac 瀏覽器驗證成功） |
| **總耗時** | **約 16 分鐘** | 兩張截圖時間差 |
| 外部驗證完成 | 12:00:40 | 截圖 11（手機 4G 驗證） |
| **總指令數** | **18 步** | 見下方步驟清單 |
| **失敗次數** | **2 次** | 見出錯紀錄 |

---

## 步驟清單（實際執行順序）

| # | 指令／動作 | 結果 |
|---|---|---|
| 1 | `export AWS_PROFILE=nkc202 AWS_DEFAULT_REGION=ap-northeast-1` | ✅ |
| 2 | `aws ssm start-session --target i-0937...` | ✅ 連線成功 |
| 3 | `sudo -i && mkdir -p /opt/app && cd /opt/app && pwd` | ❌ **失敗 1** |
| 4 | 一次貼上完整 app.py（58 行 heredoc） | ❌ **失敗 2** |
| 5 | `wc -l /opt/app/app.py`（驗證） | ❌ 檔案不存在 |
| 6 | `mkdir -p /opt/app && cd /opt/app && pwd` | ✅ 目錄建立 |
| 7 | 貼上 app.py 第 1 段（import 與變數） | ✅ |
| 8 | 貼上 app.py 第 2 段（HTML 樣式） | ✅ |
| 9 | 貼上 app.py 第 3 段（HTML 內容） | ✅ |
| 10 | 貼上 app.py 第 4 段（路由函式） | ✅ |
| 11 | `wc -l` + `py_compile` 驗證 | ✅ 45 行、語法正確 |
| 12 | 建立 `requirements.txt` | ✅ |
| 13 | `python3.11 -m venv /opt/app/venv` | ✅ |
| 14 | `pip install -r requirements.txt` | ✅ 約 1 分鐘 |
| 15 | **人工填寫** `/opt/app/env`（版本號、commit、時間） | ✅ |
| 16 | 建立 `/etc/systemd/system/nkc202app.service` | ✅ |
| 17 | `daemon-reload` + `enable --now` + `status` | ✅ active (running) |
| 18 | `curl -s localhost`（機器內驗證） | ✅ 回傳 HTML |

**合計 18 步，其中 3 步是因為前面失敗而重做的。**

---

## 出錯紀錄

| # | 在哪一步 | 錯誤訊息 | 原因 | 怎麼解決 |
|---|---|---|---|---|
| 1 | 步驟 3<br>建立目錄 | 無錯誤訊息，但提示符停在 `[root@... ~]#`，目錄未建立 | **`sudo -i` 會開啟新的互動式 root shell，後面用 `&&` 串接的指令不會在新 shell 中執行**，而是排隊等該 shell 結束後才跑 | 切成 root 之後才另外下 `mkdir` 指令 |
| 2 | 步驟 4<br>貼上 app.py | 畫面出現 `[PYEOFpp.run(host=...` 等字元錯亂，接著 `-bash: /opt/app/app.py: No such file or directory` | **Session Manager 走 WebSocket 傳輸，一次貼入 58 行會掉字**，heredoc 結束標記被吃掉導致整段失效 | 拆成 4 段分次貼入，每段約 10–15 行 |

---

## 過程中實際感受到的問題

1. **沒有 scp、沒有檔案傳輸機制**，程式碼只能整段貼進終端機，長內容一定會貼壞。這是「把程式送上伺服器」這件事本身就缺乏可靠工具。
2. **版本號 `BUILD_VERSION=v1.0.0-manual` 是人工隨手填的**，沒有任何機制保證它與實際執行的程式碼一致。可以改了程式卻忘記改版本號，也可以把版本號填成任意值。
3. **`GIT_COMMIT` 只能填 `none-manual-deploy`** —— 手動部署根本不知道自己部署的是哪一版程式碼。這代表**無法追溯、無法回滾**。
4. **每改一行程式碼，上述 18 個步驟必須從頭再做一次。**
5. **沒有測試關卡**：程式碼有沒有壞，只能靠人工執行 `py_compile` 檢查語法，且完全依賴操作者記得要做。
6. **環境依賴需人工處理**：要自己判斷該用 venv、要自己記得裝哪些套件、版本要自己對。換一台機器就要重來一次，且無法保證環境一致。
7. **全程仰賴操作者不出錯**：18 個步驟沒有任何一步有防呆，漏一步或順序錯了，錯誤往往到最後才浮現。

---

## 這些問題分別由哪一關解決

| 關 0 的問題 | 哪一關解決 | 怎麼解決 |
|---|---|---|
| 程式碼送不上去、貼上會壞 | 關 1 | 打包成 Docker 映像檔，用 `docker pull` 傳輸，不再手貼 |
| 環境依賴要人工處理、換機器就重來 | 關 1 | 映像檔內含完整環境，任何機器跑起來都一樣 |
| 版本號人工填、可能不一致 | 關 2 | GitHub Actions 自動以 git commit SHA 標記映像檔 |
| 不知道線上是哪一版、無法回滾 | 關 2–3 | 每個映像檔綁定 commit SHA，可指定舊版重新部署 |
| 沒有測試關卡 | 關 2 | pipeline 內建 pytest，測試不過直接中止，不准上線 |
| 打包要人工 | 關 2 | push 即自動 build |
| 部署要人工 18 步 | 關 3 | ECR 更新後自動觸發 SSM 部署，人工介入為零 |
| 換版會中斷服務 | 關 4 | ECS Fargate 滾動更新，新任務健康後才撤舊任務 |

---

## 對照基準（報告用）

| | 步驟數 | 耗時 | 人工介入 | 出錯次數 | 可否追溯版本 |
|---|---|---|---|---|---|
| **關 0（手動）** | **18 步** | **16 分鐘** | **每次都要** | **2 次** | **否** |
| 關 1（容器化） | 待填 | | | | |
| 關 2（自動 build） | 待填 | | | | |
| 關 3（全自動） | 待填 | | | | |

> 這張表是整份報告的結論。關 3 完成後預期為「1 步（git push）、約 3 分鐘、零人工介入、可追溯到 commit」。
