# 期末專題 選題說明書

**姓名：** 張博堯
**班級：** NKC202【第 4 期】AWS 雲端工程師（在職遠距）
**AWS 帳號：** （帳號 ID 已遮蔽，繳交時另附）
**操作區域：** Asia Pacific (Tokyo) ap-northeast-1
**提交日期：** 2026 年 8 月 15 日

---

## ① 題目

**把一支應用程式的部署流程，從「手動 SSH 上機器複製檔案」一路演化到「改一行 code 就自動上線」，並在每一關留下可比對的證據。**

- 對應題號：**P3 CI/CD 演化闖關（Tier 3）**
- 目標關卡：**關 0 → 關 3**（關 4 ECS Fargate 為行有餘力之延伸）
- 解決誰的問題：軟體開發與維運團隊。手動部署慢、容易漏步驟、出錯時不知道上線的到底是哪一版；每次改一個小地方都要人工重跑一次流程。
- 一句話：**先親身體會手動部署的痛，再一關一關把痛拿掉。**

### 為什麼選這題

P3 的設計是六關遞進、**停在哪關就拿哪關的分**，屬於天然分層的題型。這代表每一關結束時的成果都是完整、可獨立驗收的，任何一週落後都不會導致整個專題無法交付。

同時，本題所需的核心技術在課程中皆已修習完畢：

| 關卡需要 | 課程對應 |
|---|---|
| Docker、Dockerfile、映像檔管理 | 第 10–11 週「Docker 容器管理」 |
| Git、分支、push 觸發 | 第 3 週「Git 版本控制」 |
| Python / Flask 應用 | 第 1–3 週「Python」 |
| VPC、Subnet、Security Group | 課程 2「AWS VPC」 |
| IAM Role 與最小權限 | 課程 1「AWS IAM」 |
| SSM Run Command 遠端下令 | 先前 Lab 已實作 |

真正的新學習項目僅有 **ECR**、**GitHub Actions**、**OIDC 免金鑰認證** 三項。

### 展示用的應用程式

刻意選用極簡的 Flask 應用（約 20 行），首頁顯示三項資訊：

```
Build Version : v1.0.3
Git Commit    : a3f9c21
Deployed At   : 2026-09-05 14:22:10 UTC
```

理由：本題的主角是「流水線」而非「程式功能」，應用程式越簡單，越能把注意力集中在部署自動化上。而把版本號做成畫面上看得到的東西，是為了讓 Demo 當下**改一行 code → 重整瀏覽器 → 數字真的變了**，讓評分者一眼就能確認流水線真的跑通，不需要看 log。

---

## ② 系統架構

### 關 3 完成時的架構圖（本專題主軸）

```
   開發者本機
      │
      │ git push origin main
      ▼
  ┌─────────────────────────────────────────────┐
  │  GitHub Repository                          │
  │   ├─ app.py / requirements.txt              │
  │   ├─ Dockerfile                             │
  │   └─ .github/workflows/deploy.yml           │
  └──────────────────┬──────────────────────────┘
                     │ push 事件觸發
                     ▼
  ┌─────────────────────────────────────────────┐
  │  GitHub Actions Runner                      │
  │   1. Checkout 程式碼                         │
  │   2. 執行測試（pytest）── 失敗即中止          │
  │   3. docker build → tag: <git-sha> + latest │
  │   4. 以 OIDC 取得 AWS 臨時憑證                │
  │      （repo 內不存放任何長期金鑰）             │
  │   5. docker push → ECR                      │
  │   6. aws ssm send-command → 觸發部署          │
  └──────────────────┬──────────────────────────┘
                     │
   ══════════════════╪═══════ AWS ap-northeast-1 ═══════════
                     │
  ┌──────────────────▼──────────────────────────┐
  │  Amazon ECR（私有 registry）                 │
  │   myapp:latest / myapp:a3f9c21              │
  │   Lifecycle Policy：只保留最近 5 個版本        │
  └──────────────────┬──────────────────────────┘
                     │ docker pull
  ┌──────────────────┼──────────────────────────────────┐
  │ VPC 10.0.0.0/16  │                                  │
  │                  │                                  │
  │  ┌───────────────▼──────────────────────────────┐   │
  │  │ Public Subnet 10.0.1.0/24 (AZ 1a)            │   │
  │  │  ┌────────────────────────────────────────┐  │   │
  │  │  │ EC2 t3.micro                           │  │   │
  │  │  │  SSM Agent ← 接收 Run Command           │  │   │
  │  │  │  執行：docker pull + 重啟容器            │  │   │
  │  │  │  Docker restart policy: always         │  │   │
  │  │  │                                        │  │   │
  │  │  │  SG(app-sg)：只開 80                    │  │   │
  │  │  │              不開 22 port               │  │   │
  │  │  │  IAM Role：ECR 唯讀 + SSM 受管           │  │   │
  │  │  └────────────────────────────────────────┘  │   │
  │  └──────────────────────────────────────────────┘   │
  └─────────────────────────────────────────────────────┘
                     │
                     ▼
              使用者瀏覽器
       看到版本號已更新為新的 commit
```

### 關 4 延伸架構（行有餘力）

```
  GitHub Actions
       └─ 更新 ECS Task Definition
              └─► ECS Fargate Service（Rolling Update）
                     ├─ Task 1（新版本）
                     └─ Task 2（新版本）
                        ↑ 舊 Task 待新 Task 健康檢查通過後才下線
                          → 部署過程中服務不中斷
```

### 設計說明

| 設計決策 | 理由 |
|---|---|
| **GitHub Actions 使用 OIDC 而非 Access Key** | repo 內完全不存放長期金鑰，改由 GitHub 與 AWS 建立信任關係換取臨時憑證。直接對應評分維度③「金鑰不落 repo」 |
| **關 3 的自動部署以 SSM Run Command 觸發** | 不需在 EC2 上開任何對外連接埠，也不需在 GitHub 存放 SSH 私鑰。相較於「Actions 用 SSH 連進去下指令」的常見作法，攻擊面小得多 |
| **EC2 不開 22 port** | 管理一律走 Session Manager，符合企業「零對外 SSH」稽核要求 |
| **映像檔同時打 `latest` 與 `<git-sha>` 兩個 tag** | `latest` 供部署使用；`<git-sha>` 讓任何一次上線都能回溯到確切的程式碼版本，出事時可立即回滾 |
| **ECR 設 Lifecycle Policy 只留 5 版** | 避免映像檔無限累積產生儲存費用 |
| **Docker `restart: always`** | 對應評分維度④「重開機不掛、自動重啟」 |
| **自建 VPC 而非 default VPC** | 才能自行控制網段切分，也才有 VPC 與 Subnet 的截圖可供繳交 |
| **關 5 跨雲不做** | 需另開 GCP 帳號並管理第二套憑證，時程與費用不划算。於報告中說明評估過程與取捨理由 |

### 使用的 AWS 服務

VPC / Subnet / Internet Gateway / Route Table / Security Group / EC2 /
ECR / IAM（Role、OIDC Identity Provider）/ Systems Manager（Session Manager、Run Command）/
CloudWatch Logs / Budgets ／（延伸：ECS Fargate、Application Load Balancer）

---

## ③ 預期成效

**做完能達成什麼**

- 能建出一條完整的自動部署流水線：改一行程式碼、push 上去，數分鐘後線上服務即更新，全程無人工介入
- 能說明每一關解決了前一關的什麼痛點，以及為什麼業界要往這個方向演化
- 能在不存放任何長期金鑰、不開放任何 SSH 連接埠的前提下完成自動部署
- 每一次上線都可追溯到確切的 Git commit，具備回滾能力

**學到什麼能力**

| 能力 | 對應職務 |
|---|---|
| 容器化與映像檔版本管理 | 現代應用交付的基本單位 |
| CI/CD 流水線設計與實作 | DevOps 工程師的核心職能 |
| 雲端身分聯邦（OIDC）與最小權限 | 資安稽核必問，也是外部系統整合的正解 |
| 免 SSH 的部署與主機管理 | 企業普遍要求「零對外 SSH」 |
| 部署可追溯性與回滾機制 | 上線出事時的第一道保險 |

**對職場有何幫助**

CI/CD 是現代開發團隊的日常標配，直接決定團隊的交付速度。面試時「你們公司怎麼上線？」幾乎必問，而本專題能完整回答：從手動部署的痛點出發，每一關為什麼要這樣改、改完解決了什麼問題。**「六關的前後對照」本身就是最好的面試素材**——不只是會用工具，而是說得出演化的理由。

---

## ④ 甘特圖時程（3 週，8/15 – 9/7）

```
                             W1            W2            W3          繳交
任務                      8/15–8/22    8/23–8/29    8/30–9/5     9/6–9/7
────────────────────────────────────────────────────────────────────────
【設計】
 需求釐清 & 架構設計       ████
 Budgets 告警設定          ██
【基礎環境】
 VPC / 網段（CloudFormation）  ████
 EC2 + IAM Role + SSM 驗證     ████
【關 0：手動部署】
 Flask 應用撰寫                ████
 手動上機部署（記錄痛點）        ████
                                  ◆ 關 0 完成
【關 1：容器化 + ECR】
 Dockerfile 撰寫                  ██████
 建立 ECR、推送映像檔               ██████
 EC2 拉取並執行容器                   ████
                                       ◆ 關 1 完成
【關 2：GitHub Actions 自動 Build】
 repo 建立 & OIDC 信任設定              ██████
 workflow 撰寫（build + test + push）     ████████
                                              ◆ 關 2 完成
【關 3：自動部署到 EC2】
 SSM Run Command 部署腳本                    ██████
 端到端驗證：改 code → 自動上線                  ██████
                                                 ◆ 關 3 完成＝主軸達標
【關 4：ECS Fargate（延伸）】
 ECS Cluster / Task Def / Service                  ████████
 Actions 改為部署至 ECS                               ██████
【交付】
 各關證據截圖整理                                    ████████
 架構圖 & README                                     ████████
 Demo 演練                                              ████
 專題報告定稿                                              ██████
────────────────────────────────────────────────────────────────────────
里程碑           ◆M1           ◆M2           ◆M3          ◆M4
                 8/22          8/29          9/5          9/7
                 關0+關1       關2+關3        驗收          繳交
                              ↑ Tier 3 成立
```

### 里程碑定義

| 里程碑 | 日期 | 達成條件 |
|---|---|---|
| **M1** | 8/22 | **關 0、關 1 完成**。應用可手動部署到 EC2；已容器化並推上 ECR，EC2 能拉取執行 |
| **M2** | 8/29 | **關 2、關 3 完成 → Tier 3 主軸成立**。git push 後全自動 build、推 ECR、部署上線，人工零介入 |
| **M3** | 9/5 | 六關證據截圖齊備、架構圖與 README 完成；驗收當天現場示範「改一行 code → 自動上線」 |
| **M4** | 9/7 | 專題報告、GitHub repo 全部繳交 |

### 風險與備案

| 風險 | 備案 |
|---|---|
| OIDC 信任設定卡關 | 先以 IAM User Access Key 存入 GitHub Secrets 打通流程，確認流水線可跑之後再改為 OIDC。**流程優先，安全性強化排後面** |
| GitHub Actions 免費額度不足 | 公開 repo 的 Actions 免費額度無上限；若使用私有 repo 則改為公開 |
| 第 3 週時間不足 | 關 3 已達 Tier 3，關 4 直接放棄，把時間全部投入文件與 Demo 演練 |
| ECR 或 ECS 受 Free Plan 限制 | 比照 8/1 實作紀錄作法：於文件標示限制、附上證據畫面，並說明替代方案 |
| 費用失控 | 開工前先設 Budgets 告警；非施作時段停用 EC2；ECR 設 Lifecycle Policy；Demo 後全數 terminate |

**費用預估：** EC2 t3.micro（Free Tier 範圍內）＋ ECR 儲存（映像檔約 200MB，僅留 5 版）＋ GitHub Actions（免費）
→ **全程預估 US$10 以內**，遠低於帳戶餘額。

---

## ⑤ 檢核點（可逐條打勾）

### 關 0：手動部署到 EC2（體會痛）

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 1 | Flask 應用可於本機執行，首頁顯示版本號 | W1 | 本機瀏覽器畫面 | ☐ |
| 2 | 以人工方式將程式送上 EC2 並啟動 | W1 | 操作過程截圖 | ☐ |
| 3 | 外部瀏覽器連得到網站 | W1 | 以手機行動網路開啟網站 | ☐ |
| 4 | **記錄手動部署的步驟數與耗時** | W1 | 文件中列出步驟清單與計時，作為後續各關的改善對照基準 | ☐ |

### 關 1：Dockerfile → 推 ECR → EC2 拉取

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 5 | Dockerfile 完成，本機 build 與 run 成功 | W1 | `docker images` 與容器執行畫面 | ☐ |
| 6 | 建立 ECR 私有 repository | W1 | ECR Console 截圖 | ☐ |
| 7 | 映像檔成功推上 ECR，含 latest 與 git-sha 雙 tag | W1 | ECR 映像檔清單截圖 | ☐ |
| 8 | EC2 從 ECR 拉取並執行容器，網站可存取 | W1 | EC2 上 `docker ps` ＋ 瀏覽器畫面 | ☐ |
| 9 | EC2 以 IAM Role 存取 ECR，未使用長期金鑰 | W1 | IAM Role 政策截圖 | ☐ |

### 關 2：push GitHub → Actions 自動 Build → 進 ECR

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 10 | GitHub repo 建立，程式碼與 Dockerfile 已納管 | W2 | repo 連結 | ☐ |
| 11 | workflow 於 push 時自動觸發 | W2 | Actions 執行紀錄截圖 | ☐ |
| 12 | pipeline 內含測試步驟，測試失敗會中止部署 | W2 | **故意寫錯測試，展示 pipeline 紅燈中止的畫面** | ☐ |
| 13 | Actions 自動 build 並推送映像檔進 ECR | W2 | Actions log ＋ ECR 新映像檔時間戳 | ☐ |
| 14 | 以 OIDC 取得 AWS 憑證，repo 內無任何長期金鑰 | W2 | IAM Identity Provider 設定 ＋ workflow 檔內容 | ☐ |

### 關 3：ECR 更新 → 自動部署到 EC2

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 15 | Actions 透過 SSM Run Command 觸發 EC2 部署 | W2 | Run Command 執行紀錄 | ☐ |
| 16 | EC2 自動拉取新映像檔並重啟容器 | W2 | 部署前後 `docker ps` 的 CONTAINER ID 對照 | ☐ |
| 17 | **端到端：改一行 code → push → 網站版本號自動更新** | W2 末 | 錄影或前後截圖，含 commit 時間與網站顯示時間 | ☐ |
| 18 | 全程無任何人工登入機器的動作 | W2 末 | 說明流程並對照關 0 的步驟清單 | ☐ |
| 19 | app-sg 全程不開 22 port | W2 | SG 輸入規則截圖 | ☐ |
| 20 | 可回滾：指定舊 git-sha 的映像檔重新部署 | W2 末 | 回滾操作後網站版本號變回舊版的畫面 | ☐ |

### 關 4：部署到 ECS Fargate（延伸，非必要）

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 21 | 建立 ECS Cluster、Task Definition、Service | W3 | ECS Console 截圖 | ☐ |
| 22 | Actions 改為更新 ECS 服務 | W3 | Actions log ＋ ECS 部署事件 | ☐ |
| 23 | Rolling update 過程中服務不中斷 | W3 | 部署期間持續請求網站皆回 200 的紀錄 | ☐ |

### 文件與交付

| # | 檢核項目 | 期限 | 驗收方式 | ✓ |
|---|---|---|---|---|
| 24 | GitHub repo 含架構圖 | 9/5 | repo 連結 | ☐ |
| 25 | GitHub repo 含每一關的成功證據截圖 | 9/5 | repo 連結 | ☐ |
| 26 | 含 VPC 截圖、EC2 所屬 VPC 與 Subnet 截圖 | 9/5 | repo 連結 | ☐ |
| 27 | README 說得出每一關「解決了什麼痛點」 | 9/5 | README 演化章節 | ☐ |
| 28 | **六關前後對照表**：步驟數、耗時、人工介入次數 | 9/5 | 報告中的對照表 | ☐ |
| 29 | 開帳先設 Budgets 告警 | W1 初 | Budgets 設定截圖 | ☐ |
| 30 | Demo 後資源全數 terminate | 9/5 後 | 清理後的資源查詢結果 | ☐ |

---

## 附註：本專題的核心論述

本專題不是「學會用 GitHub Actions」，而是回答一個問題：**為什麼業界要花力氣把部署自動化？**

因此檢核點 #4 特別要求記錄關 0 手動部署的步驟數與耗時，並在 #28 做成六關對照表。預期呈現的結果類似：

| | 步驟數 | 耗時 | 人工介入 | 出錯風險 |
|---|---|---|---|---|
| 關 0 手動 | 約 12 步 | 約 15 分鐘 | 每次都要 | 高，易漏步驟 |
| 關 1 容器化 | 約 6 步 | 約 8 分鐘 | 每次都要 | 中，環境已固定 |
| 關 2 自動 Build | 約 2 步 | 約 4 分鐘 | 仍需手動部署 | 低 |
| 關 3 全自動 | **1 步（git push）** | **約 3 分鐘** | **零** | **低，且可回滾** |

這張表就是整個專題的結論，也是 Demo 當天最能講清楚價值的一頁。
