# 關 1：Dockerfile → ECR → EC2 拉取

**日期：** 2026-08-16
**目標：** 解決關 0 記錄的痛點——程式碼傳輸不可靠、環境需人工安裝、版本無法追溯

---

## 這一關解決了關 0 的哪些問題

| 關 0 記錄的問題 | 關 1 的解法 | 驗證結果 |
|---|---|---|
| 程式碼手貼會壞（失敗 2 次） | `docker pull` 完整傳輸映像檔 | ✅ 傳輸零失敗 |
| 要人工建 venv、裝套件 | 環境打包進映像檔 | ✅ EC2 上未安裝任何套件 |
| 換台機器就要全部重來 | 同一映像檔在任何機器行為一致 | ✅ Mac build、EC2 執行 |
| 不知道線上是哪一版 | 版本資訊烤進映像檔 + tag 標記 | ✅ `GIT_COMMIT=1cfbef1` |
| 18 步、16 分鐘 | 7 步、37 秒 | ✅ |

---

## 計量結果

| 項目 | 數值 | 依據 |
|---|---|---|
| 部署步驟 | **7 步** | 見下方清單 |
| build 時間 | 17.7 秒 | `time docker build` |
| push 時間 | 10.6 秒 | `time docker push` |
| EC2 pull 時間 | 3.5 秒 | SSM 執行輸出 |
| 容器啟動 | 約 5 秒 | `docker ps` 狀態 |
| **機器執行總時間** | **約 37 秒** | 上述加總 |
| 映像檔大小 | 47.4 MB | `docker images` |
| 傳輸失敗次數 | **0 次** | |
| 版本可追溯 | **是**（git commit `1cfbef1`） | |

### 步驟清單

| # | 動作 | 在哪執行 |
|---|---|---|
| 1 | `docker build --platform linux/amd64 --provenance=false ...` | 本機 Mac |
| 2 | `aws ecr get-login-password \| docker login` | 本機 Mac |
| 3 | `docker push :v1.1.1-docker` | 本機 Mac |
| 4 | `docker push :latest` | 本機 Mac |
| 5 | `aws ecr get-login-password \| docker login` | EC2（經 SSM） |
| 6 | `docker pull :v1.1.1-docker` | EC2（經 SSM） |
| 7 | `docker rm -f && docker run -d --restart always -p 80:8080` | EC2（經 SSM） |

---

## 出錯紀錄

| # | 問題 | 錯誤訊息 | 根本原因 | 解法 |
|---|---|---|---|---|
| 1 | 跨架構 build（事前預防，未實際觸發） | 若未處理會出現 `exec format error` | **本機 Mac 為 arm64（Apple Silicon），EC2 t3.micro 為 x86_64**。映像檔內含已編譯的機器碼，兩種架構不相容 | build 時指定 `--platform linux/amd64` |
| 2 | ECR 漏洞掃描沒有結果 | `ScanNotFoundException: Image scan does not exist` | buildx 搭配 `--platform` 預設會產出 **OCI Image Index**（映像檔索引 + attestation 中繼資料），而 **ECR 基本掃描只支援單一映像檔 manifest，不掃索引** | build 時加上 `--provenance=false --sbom=false`，產出單一架構 manifest（`manifest.v2+json`）後掃描即正常運作 |

---

## 安全性成果（評分維度③）

| 措施 | 實作方式 | 意義 |
|---|---|---|
| 私有映像檔倉庫 | ECR Private | 程式碼不對外公開 |
| **推送時自動漏洞掃描** | `scanOnPush=true` | **實際掃出 CRITICAL 4 / HIGH 8 / MEDIUM 6**，來源為基礎映像檔的 Debian 系統套件 |
| 容器以非 root 執行 | Dockerfile 建立 `appuser` 並 `USER appuser` | 容器遭入侵時攻擊者非 root |
| 容器內不綁定特權 port | 內部監聽 8080，對外由 `-p 80:8080` 映射 | 非 root 無法綁定 1024 以下 port，此設計與上一項互相配合 |
| **拉取映像檔不使用任何帳密** | EC2 以 IAM Role（`AmazonEC2ContainerRegistryReadOnly`）換取臨時權杖 | 主機上不存放任何長期憑證 |
| 生命週期政策 | 只保留最近 5 個映像檔 | 避免無限累積產生儲存費用（維度④） |

> **待改善**：掃出的 CRITICAL 漏洞來自基礎映像檔。實務上的處理方式是定期更新基底（例如改用更新的 `python:3.12-slim` 或加上 `apt-get upgrade`），並把掃描結果納入 CI 的檢查條件。此項列為後續改善建議。

---

## 這一關還沒解決的問題（留給關 2、關 3）

1. **build 與 push 仍是人工下指令** → 關 2 由 GitHub Actions 自動執行
2. **版本號仍是人工決定**（`v1.1.1-docker` 是我打的） → 關 2 改由 git 自動帶入
3. **沒有測試關卡**，程式壞掉照樣可以推上去 → 關 2 加入 pytest 門檻
4. **部署到 EC2 仍需人工下 pull + run** → 關 3 由 ECR 更新自動觸發

---

## 累計對照表

| | 步驟數 | 執行時間 | 人工介入 | 失敗次數 | 版本可追溯 |
|---|---|---|---|---|---|
| **關 0（手動）** | 18 步 | 16 分鐘 | 每次都要 | 2 次 | ❌ |
| **關 1（容器化）** | **7 步** | **37 秒** | 每次都要 | 0 次 | ✅ `1cfbef1` |
| 關 2（自動 build） | 待填 | | | | |
| 關 3（全自動） | 待填 | | | | |

**關 0 → 關 1 改善幅度：步驟數 −61%、執行時間 −96%、傳輸失敗歸零、版本從無法追溯變為可追溯。**
