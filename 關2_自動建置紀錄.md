# 關 2：push GitHub → Actions 自動 Build → 進 ECR

**日期：** 2026-08-16
**目標：** 解決關 1 剩下的問題——build/push 仍需人工、版本號人工決定、沒有測試關卡

---

## 這一關解決了什麼

| 關 1 剩下的問題 | 關 2 的解法 | 驗證 |
|---|---|---|
| build 與 push 要人工打指令 | `git push` 後 GitHub Actions 自動執行 | ✅ 第 6 次執行全綠 |
| 版本號人工決定（`v1.1.1-docker` 是手打的） | 由 `github.run_number` 與 commit SHA 自動產生 | ✅ `v1.2.6-ci` / `4649dca` |
| 沒有測試關卡，壞的程式照樣推得上去 | 測試獨立成 job，`needs: test` 擋住後續 | ✅ 測試不過則 build 不執行 |
| 本機要有 AWS 憑證才能推 ECR | OIDC 換取臨時憑證 | ✅ repo 內零金鑰 |

---

## 計量結果

| 項目 | 數值 |
|---|---|
| **人工步驟** | **1 步（`git push`）** |
| 流水線執行時間 | **56 秒**（測試 8 秒 + 建置推送 48 秒） |
| 人工介入 | **零** |
| 版本可追溯 | ✅ commit SHA、run number、觸發者全部自動記錄 |
| 產出標籤 | `4649dca`（commit）、`v1.2.6-ci`（版本）、`latest`（部署用） |

---

## 安全設計：OIDC 免金鑰認證

```
GitHub Actions 執行
  → GitHub 為這次執行簽發身分權杖（JWT，有效期數分鐘）
  → 向 AWS 出示：「我是 CarlosChangYao/aws-cicd-pipeline 的 main 分支」
  → AWS 依信任政策驗證後，發給臨時憑證（1 小時失效）
  → 用完即棄
```

**三層限制：**

| 層次 | 設定 | 效果 |
|---|---|---|
| 誰能取得憑證 | 信任政策鎖定 `repo:CarlosChangYao@39613248/aws-cicd-pipeline@1334878950:*` | 其他 repo 無法冒充 |
| 能做什麼 | 僅 `ecr:*` 且資源限定 `nkc202-cicd-app` 單一倉庫 | 不能碰 EC2、S3 或建立資源 |
| 有效多久 | 1 小時 | 即使外洩也很快失效 |

> 刻意**未**授予 SSM 權限——關 3 需要時才追加，實踐最小權限原則。

---

## 除錯歷程（本關最有價值的部分）

第一次執行到成功，共花了 **6 次執行**。錯誤訊息始終是同一句：

```
Error: Could not assume role with OIDC: Not authorized to perform sts:AssumeRoleWithWebIdentity
```

| 次數 | 耗時 | 做了什麼 | 結果 |
|---|---|---|---|
| 1 | 108 秒 | 首次執行 | ❌ 失敗 |
| 2 | 146 秒 | **推測①：IAM 傳播延遲**，重新觸發 | ❌ 推測錯誤 |
| 3 | 81 秒 | **加入除錯步驟**，印出 OIDC 權杖的 claims | ❌ 仍失敗，但取得關鍵資料 |
| 4 | 177 秒 | **推測②：憑證指紋過期**，更新為即時抓取的指紋 | ❌ 推測錯誤 |
| 5 | 97 秒 | **推測③：大小寫不符**，信任政策同時接受兩種寫法 | ❌ 推測錯誤 |
| 6 | 56 秒 | **讀除錯輸出後對症下藥** | ✅ **成功** |

### 真正的原因

除錯步驟印出 GitHub 實際送出的身分內容：

```
sub = repo:CarlosChangYao@39613248/aws-cicd-pipeline@1334878950:ref:refs/heads/main
```

而信任政策當初設定的比對條件是：

```
repo:CarlosChangYao/aws-cicd-pipeline:*
```

**GitHub 已改用「不可變主體識別（immutable subject）」格式**，在 `sub` 中嵌入帳號與 repo 的數字 ID。網路上絕大多數教學文件仍使用舊格式，照抄必然失敗。

修正後的信任政策：

```
repo:CarlosChangYao@39613248/aws-cicd-pipeline@1334878950:*
```

> **改用數字 ID 反而更安全**：帳號名稱與 repo 名稱都可以改，數字 ID 不會變。即使有人日後註冊同名帳號與 repo，也無法通過驗證。

### 這段經歷的啟示

**三次推測全錯，一次量測直接命中。**

前五次共花費 609 秒（約 10 分鐘）在猜測與驗證推測上；除錯步驟印出資料後，問題在數秒內定位完成。

錯誤訊息 `Not authorized` 本身資訊量極低——它只說「不被允許」，不說「哪一項條件不符」。這種情境下**唯一有效的方法是把雙方實際的值都印出來直接比對**，而非依經驗猜測。

這也印證了關 0 建立的工作習慣：**不憑記憶或直覺下結論，動手驗證。**

---

## 這一關還沒解決的問題（留給關 3）

映像檔已自動進入 ECR，但**EC2 上跑的仍是舊版本**——因為還沒有任何機制通知 EC2 去拉新映像檔。

目前狀態：

```
git push → 測試 → build → 推 ECR → 【斷點】→ EC2 手動 pull + run
```

關 3 要補上的就是那個斷點：**ECR 更新後自動觸發 EC2 部署**，預計以 GitHub Actions 呼叫 SSM Run Command 完成（不開 SSH、不在 GitHub 存放主機憑證）。

---

## 累計對照表

| | 人工步驟 | 執行時間 | 人工介入 | 測試關卡 | 版本可追溯 |
|---|---|---|---|---|---|
| **關 0（手動）** | 18 步 | 16 分鐘 | 每次都要 | ❌ 無 | ❌ |
| **關 1（容器化）** | 7 步 | 37 秒 | 每次都要 | ❌ 無 | ✅ 手動帶入 |
| **關 2（自動建置）** | **1 步** | **56 秒** | **零** | ✅ 自動阻擋 | ✅ 自動產生 |
| 關 3（全自動部署） | 待填 | | | | |

**關 0 → 關 2 改善幅度：人工步驟 18 → 1（−94%）、人工介入歸零、新增自動化測試關卡、版本從無法追溯變為機器自動產生。**
